# routing-optimization-lab
# Install with: pip install -r requirements.txt

numpy>=1.24
matplotlib>=3.7
ortools>=9.7        # Google OR-Tools — used from Step 4 onwards

# UI
streamlit>=1.35     # interactive visual dashboard
fastapi>=0.111      # REST API layer
uvicorn>=0.29       # ASGI server to run FastAPI
pydantic>=2.0       # request/response validation (FastAPI dependency)
